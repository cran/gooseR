% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/goose_ask_enhanced.R
\name{goose_ask_raw}
\alias{goose_ask_raw}
\title{Original goose_ask (unformatted)}
\usage{
goose_ask_raw(
  prompt,
  output_format = c("text", "json"),
  quiet = TRUE,
  timeout = getOption("goose.timeout", 300),
  retries = getOption("goose.retries", 1),
  session_id = NULL
)
}
\arguments{
\item{prompt}{Character string with the question or prompt}

\item{output_format}{Character, either "text" or "json"}

\item{quiet}{Logical, suppress status messages}

\item{timeout}{Numeric, timeout in seconds (default 300, i.e., 5 minutes).
Complex queries may take longer. Set to Inf for no timeout.}

\item{retries}{Integer, number of retries after the first attempt (default uses
\code{getOption('goose.retries', 1)}). Retries only occur for timeout/transient errors.}

\item{session_id}{Optional session ID for context preservation}
}
\value{
Character string with response (text format) or list (json format)
}
\description{
This is the original goose_ask function without formatting.
Use this if you need the raw behavior.
}
